import Foundation

/// Example enpoints for APIRequest. Return string coresponding to enpoint
enum APIEndpoint: String {
    case repositories = "search/repositories" // rawValue is "search/repositories"
    case users  // rawValue is "users"
}
